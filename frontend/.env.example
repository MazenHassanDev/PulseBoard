# Frontend (Vite) environment variables.
# Copy to .env for local development. In production these are set on Render.

# Base URL of the Django API.
VITE_API_URL=http://localhost:8000

# Shared site access password. When set, the whole app sits behind a single
# password gate. Leave empty in local dev to bypass the gate.
VITE_ACCESS_PASSWORD=
